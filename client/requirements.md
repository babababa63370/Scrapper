## Packages
prismjs | Syntax highlighting for HTML code blocks
date-fns | Formatting dates for the dashboard

## Notes
The extension will communicate with POST /api/pages to save scraped content.
We need to display the raw HTML content in a readable format using prismjs.
